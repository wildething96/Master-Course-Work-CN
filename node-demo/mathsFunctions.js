const multiply = (a,b) => {
    return a*b;
    }
    
const add = (a,b) => {
    return a+b;
}

module.exports = {
    name : "Maths functions",
    description : "this is some code to help with maths",
    add,
    multiply
}